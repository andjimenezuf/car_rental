import { MantineGradient } from "@mantine/core";

export const primaryGradient: MantineGradient = {from: 'blue', to:'purple'};

export const secondaryGradient: MantineGradient = {from: 'red', to:'purple'};

export const USCurrency = '$';
